# 聚财中发交易系统

基于 React + TypeScript + Vite 构建的金融交易管理系统，提供基金、期权、合约交易及用户管理功能。

## 项目概述

聚财中发交易系统是一个现代化的金融交易平台，包含：
- **用户端**：交易面板、资金记录、基金详情等
- **管理端**：基金/期权/合约/用户管理、数据导出等
- **统一组件**：Toast通知、表单校验、错误处理等

## 最新进度 (2025-02-15)

### ✅ 已完成功能
- **核心交易模块**：基金、期权、合约交易页面
- **用户功能**：交易面板、资金记录、基金详情
- **管理功能**：AdminFunds、AdminOptions、AdminContracts、AdminUsers
- **统一体验**：
  - Toast通知系统、表单校验（required/maxLength/数值校验）
  - ErrorBoundary错误边界统一处理
  - 全局样式与主题系统
- **数据处理**：
  - Excel导出、历史数据生成
  - Supabase数据库集成与连接状态管理
  - 数据服务层CRUD操作封装

### 🔄 进行中
- 性能优化与代码分割
- 多语言国际化支持
- 移动端响应式适配

---

## 技术栈

- **前端框架**: React 18 + TypeScript + Vite
- **状态管理**: React Context API
- **路由管理**: React Router v6
- **样式方案**: CSS Modules + 全局样式变量
- **数据存储**: Supabase (PostgreSQL)
- **开发工具**: ESLint, Prettier
- **部署环境**: Vercel/Netlify

## 功能特性

### 1. 错误边界处理
系统集成了全局错误边界(ErrorBoundary)组件，可捕获渲染过程中的JavaScript错误，防止整个应用崩溃，并提供友好的错误提示和恢复机制。

### 2. Supabase数据集成
- 实现了完整的数据服务层，封装CRUD操作
- 提供连接状态管理和错误处理
- 支持在线/离线模式切换

### 3. 统一UI/UX体验
- 全局样式变量和主题系统
- 响应式布局支持
- 统一的表单验证和错误提示

## 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## 环境变量配置

创建`.env`文件并配置以下变量:

```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## Tailwind v4 使用说明

项目已使用 Tailwind v4 与 PostCSS 插件，正确使用方法如下：

- 在 `src/index.css` 顶部使用 v4 指令：

```css
@import "tailwindcss";
```

- 确保 PostCSS 配置正确（`postcss.config.js`）：

```js
import tailwindcss from '@tailwindcss/postcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: [
    tailwindcss(),
    autoprefixer(),
  ],
};
```

- Tailwind 配置（`tailwind.config.js`）建议包含以下扫描路径：

```js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {}
  },
  plugins: [],
}
```

- 引入顺序建议：在 `src/main.tsx` 先引入 `./index.css`，再渲染应用：

```ts
import './index.css'
import App from './App'
```

### 从 v3 迁移到 v4 的注意事项
- 替换旧指令：将 `@tailwind base; @tailwind components; @tailwind utilities;` 替换为 `@import "tailwindcss";`
- 确保安装并启用 `@tailwindcss/postcss` 插件，否则指令不会被处理
- 避免在局部样式中直接修改 `#root` 的基础布局属性，建议使用页面容器类或 Tailwind 工具类

### 常见问题与排查
- 样式未生效：检查 PostCSS 插件是否正确配置、`index.css` 是否被引入
- 样式覆盖异常：检查全局样式加载顺序，避免在 Tailwind 之后引入大量覆盖性样式

## 路由兼容说明（与 .md.bak 对齐）

为减少历史文档与当前实现的差异，已添加以下别名路由映射，旧路径会重定向到现有页面：

- 旧 `"/manager"` → 新 `"/admin"`
- 旧 `"/manager/login"` → 新 `"/login"`
- 旧 `"/manager/dashboard"` → 新 `"/admin"`
- 旧 `"/manager/users"` → 新 `"/admin/users"`
- 旧 `"/manager/funds"` → 新 `"/admin/funds"`
- 旧 `"/manager/options"` → 新 `"/admin/options"`
- 旧 `"/manager/contracts"` → 新 `"/admin/contracts"`
- 旧 `"/trading/funds"` → 新 `"/trade/funds"`
- 旧 `"/trading/options"` → 新 `"/trade/options"`
- 旧 `"/trading/contracts"` → 新 `"/trade/contracts"`

说明：以上为路径别名，权限与登录逻辑保持不变；若未登录，仍会先跳转到 `"/login"`。
